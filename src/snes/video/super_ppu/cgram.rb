# 512 B CGRAM (Colour Graphics RAM):
# Fits 512 colour palette entries, each entry has the size of a word (16 bits).

module Video
    class CGRam
        # has a palette of 256 colors, stored as 256 15-bit words in CGRAM
    end
end